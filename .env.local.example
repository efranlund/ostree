# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Site URL for magic link redirects
# For local development, this can be omitted (will use window.location.origin)
# For production, set this to your production URL (e.g., https://yourdomain.com)
# For Vercel, this will be automatically set via Vercel's environment variables
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Note: Magic link authentication is enabled by default
# Make sure to configure email settings in your Supabase dashboard
# For local development, check your Supabase Inbucket for magic link emails

